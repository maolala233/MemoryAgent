VectorIndex
===============

.. code-block:: python

   class VectorIndex(ABC):
       def upsert(self, items: list[tuple[Uid, Embedding]]) -> None: ...
       def search(
           self, query: Embedding, top_k: int
       ) -> list[tuple[Uid, float]]: ...
       def delete(self, uids: list[Uid]) -> None: ...
       def rebuild(self, items: list[tuple[Uid, Embedding]]) -> None: ...
       def dim(self) -> int: ...

Implementations: ``AdaptiveVectorIndex`` (dynamically switches between brute force ↔ FAISS), ``FAISSVectorIndex``.
